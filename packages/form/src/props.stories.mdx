import { Meta, ArgTypes } from '@storybook/addon-docs';
import {
  hidden_Input,
  hidden_Label,
  hidden_Field,
  hidden_Checkbox,
  hidden_CheckboxGroup,
  hidden_Radio,
  hidden_FormGroup,
  hidden_CurrencyInput,
} from './Form.stories';

<Meta title="Form Components/Form/Additional Props" />

# Input Props

These props can be passed to the `Input` component

<ArgTypes of={hidden_Input} />

# Label Props

These props can be passed to the `Label` component

<ArgTypes of={hidden_Label} />

# Field Props

These props can be passed to the `Field` component

<ArgTypes of={hidden_Field} />

# Checkbox Props

These props can be passed to the `Checkbox` component

<ArgTypes of={hidden_Checkbox} />

# CheckboxGroup Props

These props can be passed to the `CheckboxGroup` component

<ArgTypes of={hidden_CheckboxGroup} />

# Radio Props

These props can be passed to the `Radio` component

<ArgTypes of={hidden_Radio} />

# FormGroup Props

These props can be passed to the `FormGroup` component

<ArgTypes of={hidden_FormGroup} />

# CurrencyInput Props

These props can be passed to the `CurrencyInput` component

<ArgTypes of={hidden_CurrencyInput} />
