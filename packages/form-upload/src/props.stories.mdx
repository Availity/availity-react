import { Meta, ArgTypes } from '@storybook/addon-docs';
import { hidden_FileList, hidden_FilePicker, hidden_FilePickerBtn, hidden_UploadProgressBar } from './Upload.stories';

<Meta title="Components/Upload/Additional Props" />

# FileList Props

These props can be passed to the `FileList` component

<ArgTypes of={hidden_FileList} />

# FilePicker Props

These props can be passed to the `FilePicker` component

<ArgTypes of={hidden_FilePicker} />

# FilePickerBtn Props

These props can be passed to the `FilePickerBtn` component

<ArgTypes of={hidden_FilePickerBtn} />

# UploadProgressBar Props

These props can be passed to the `UploadProgressBar` component

<ArgTypes of={hidden_UploadProgressBar} />
