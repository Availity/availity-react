import { Meta, ArgTypes } from '@storybook/addon-docs';
import { Img } from 'react-image';
import { hidden_RSImg, hidden_RSSkeleton } from './avatar.stories';

<Meta title="Components/Avatar/Additional Props" />

# React Image Props

These props can be passed to the `Avatar` component

<ArgTypes of={hidden_RSImg} />

# React Skeleton Props

These props can be passed to the `Avatar` component as `skeletonProps`

<ArgTypes of={hidden_RSSkeleton} />
