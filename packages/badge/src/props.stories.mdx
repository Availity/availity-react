import { Meta, ArgTypes } from '@storybook/addon-docs';
import { hidden_RSBadge } from './RemovableBadge.stories';

<Meta title="Components/Badge/Removable Badge/Additional Props" />

# Badge Props

These props can be passed to the `RemovableBadge` component

<ArgTypes of={hidden_RSBadge} />
