import { Meta, ArgTypes } from '@storybook/addon-docs';
import {
  hidden_SpacesLogo,
  hidden_SpacesTile,
  hidden_SpacesBillboard,
  hidden_SpacesDisclaimer,
  hidden_SpacesAgreement,
  hidden_SpacesGhostText,
} from './spaces.stories';

<Meta title="Components/Spaces/Additional Props" />

# SpacesLogo Props

These props can be passed to the `SpacesLogo` component

<ArgTypes of={hidden_SpacesLogo} />

# SpacesTile Props

These props can be passed to the `SpacesTile` component

<ArgTypes of={hidden_SpacesTile} />

# SpacesBillboard Props

These props can be passed to the `SpacesBillboard` component

<ArgTypes of={hidden_SpacesBillboard} />

# SpacesDisclaimer Props

These props can be passed to the `SpacesDisclaimer` component

<ArgTypes of={hidden_SpacesDisclaimer} />

# SpacesAgreement Props

These props can be passed to the `SpacesAgreement` component

<ArgTypes of={hidden_SpacesAgreement} />

# SpacesGhostText Props

These props can be passed to the `SpacesGhostText` component

<ArgTypes of={hidden_SpacesGhostText} />
