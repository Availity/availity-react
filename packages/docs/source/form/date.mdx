---
title: Date
summary: Wrapper for [react-dates](https://github.com/airbnb/react-dates) to work with [formik](https://github.com/jaredpalmer/formik)
---

## Installation

```bash
npm install @availity/date @availity/form formik@^2.0.1-rc.5 react reactstrap --save
```

## Validation

See [yup](https://github.com/jquense/yup) and [@availity/yup](https://github.com/Availity/sdk-js/tree/master/packages/yup). Note to use this package with `@availity/yup` you will need to import the `moment` version as noted in the example below.

## FormikDate (Default Export)

Date picker without a `Label` or `Feedback`

### Usage

import '@availity/date/styles.scss';

```jsx live=true
import { Form } from '@availity/form';
import { Button } from 'reactstrap';
import Date from '@availity/date';

import * as yup from 'yup';
import '@availity/yup';
import '@availity/yup/moment';
import moment from 'moment';

<div className="w-100 d-flex flex-row justify-content-around align-items-center">
  <Form
    initialValues={{
      dateOfService: '',
    }}
    onSubmit={values => console.log(values)}
    validationSchema={yup.object().shape({
      dateOfService: yup.date().required(),
    })}
  >
    <Date
      id="dateOfService"
      name="dateOfService"
      min={{ value: 7, units: 'day' }}
      max={{ value: 7, units: 'day' }}
    />
    <Button color="primary" type="submit">
      Submit
    </Button>
  </Form>
</div>;
```

### Props

See [react-dates](https://github.com/airbnb/react-dates#singledatepicker) for additional props

| Prop Name                 | Types                                                                                              | Required | Default                    | Description                                                                                                                                             |
| ------------------------- | -------------------------------------------------------------------------------------------------- | -------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`name`**                | String                                                                                             | true     |                            | The name of the field. Will be the key of the selected date that comes through in the values of the `onSubmit` callback.                                |
| **`disabled`**            | Boolean                                                                                            | false    |                            | Whether the `<FormikDate />` is disabled                                                                                                                |
| **`className`**           | String                                                                                             | false    |                            | Additional styles to be added to the `<FormikDate />`                                                                                                   |
| **`min`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `max` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`max`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `min` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`calendarIcon`**        | Node                                                                                               | false    | `<Icon name="calendar" />` | Override the default icon that appears                                                                                                                  |
| **`onChange`**            | Function                                                                                           | false    |                            | Function to be run when date is chosen. Selected value is passed to function in format: `{ value }`                                                     |
| **`onPickerFocusChange`** | Function                                                                                           | false    |                            | Function to be run when focus on the input changes. Value is passed to function in format `{ focused }`                                                 |
| **`innerRef`**            | Function,Node                                                                                      | false    |                            | Ref passed to `<Input />` inside of `<FormikDate />`.                                                                                                   |
| **`format`**              | String                                                                                             | false    | `"MM/DD/YYYY"`             | How to format date value in `onSubmit` callback. Must be a format recognized by [moment](https://momentjs.com/docs/#/displaying/format/)                |
| **`data-testid`**         | String                                                                                             | false    |                            | Useful for writing unit tests with [@testing-library/react](https://github.com/testing-library/react-testing-library)                                   |
| **`datepicker`**          | Boolean                                                                                            | false    | `true`                     | Toggle whether the calendar is shown                                                                                                                    |
| **`datePickerProps`**     | Object                                                                                             | false    |                            | Props to be spread onto the datepicker component from `react-dates`                                                                                     |

## DateField

The same as `FormikDate` but with a `Label` that appears above input and a `Feedback` that appears below the input.

### Usage

```jsx live=true
import { Form } from '@availity/form';
import FormikDate from '@availity/date';
import * as yup from 'yup';
import '@availity/yup/moment';

<div className="w-100 d-flex flex-row justify-content-around align-items-center">
  <Form
    initialValues={{
      dateOfService: '',
    }}
    onSubmit={values => console.log(values)}
    validationSchema={yup.object().shape({
      dateOfService: yup.date().required(),
    })}
  >
    <DateField
      label="Date of Service"
      id="dateOfService"
      name="dateOfService"
      min={{ value: 7, units: 'day' }}
      max={{ value: 7, units: 'day' }}
    />
    <Button color="primary" type="submit">
      Submit
    </Button>
  </Form>
</div>;
```

### Props

See [react-dates](https://github.com/airbnb/react-dates#singledatepicker) for additional props

| Prop Name                 | Types                                                                                              | Required | Default                    | Description                                                                                                                                             |
| ------------------------- | -------------------------------------------------------------------------------------------------- | -------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`name`**                | String                                                                                             | true     |                            | The name of the field. Will be the key of the selected date that comes through in the values of the `onSubmit` callback.                                |
| **`label`**               | String                                                                                             | false    |                            | The text that renders inside the `Label` above the input.                                                                                               |
| **`disabled`**            | Boolean                                                                                            | false    |                            | Whether the `<FormikDate />` is disabled                                                                                                                |
| **`className`**           | String                                                                                             | false    |                            | Additional styles to be added to the `<FormikDate />`                                                                                                   |
| **`min`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `max` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`max`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `min` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`calendarIcon`**        | Node                                                                                               | false    | `<Icon name="calendar" />` | Override the default icon that appears                                                                                                                  |
| **`onChange`**            | Function                                                                                           | false    |                            | Function to be run when date is chosen. Selected value is passed to function in format: `{ value }`                                                     |
| **`onPickerFocusChange`** | Function                                                                                           | false    |                            | Function to be run when focus on the input changes. Value is passed to function in format `{ focused }`                                                 |
| **`innerRef`**            | Function,Node                                                                                      | false    |                            | Ref passed to `<Input />` inside of `<FormikDate />`.                                                                                                   |
| **`format`**              | String                                                                                             | false    | `"MM/DD/YYYY"`             | How to format date value in `onSubmit` callback. Must be a format recognized by [moment](https://momentjs.com/docs/#/displaying/format/)                |
| **`data-testid`**         | String                                                                                             | false    |                            | Useful for writing unit tests with [@testing-library/react](https://github.com/testing-library/react-testing-library)                                   |
| **`datepicker`**          | Boolean                                                                                            | false    | `true`                     | Toggle whether the calendar is shown                                                                                                                    |
| **`datePickerProps`**     | Object                                                                                             | false    |                            | Props to be spread onto the datepicker component from `react-dates`                                                                                     |

## DateRange

A date range, consisting of 2 fields, a start date and an end date, without a `Label` or `Feedback`

### Usage

import '@availity/yup';
import '@availity/yup/moment';

```jsx live=true
import React from 'react';
import { Form } from '@availity/form';
import { DateRange } from '@availity/date';
import * as yup from 'yup';
import '@availity/yup';
import '@availity/yup/moment';
import moment from 'moment';

<div className="w-100 d-flex flex-row justify-content-around align-items-center">
  <Form
    initialValues={{
      dateOfService: undefined,
    }}
    onSubmit={values => console.log(values)}
    validationSchema={yup.object().shape({
      dateOfService: yup
        .dateRange(
          {
            min: moment()
              .subtract(7, 'day')
              .format('MM/DD/YYYY'),
            max: moment()
              .add(7, 'day')
              .format('MM/DD/YYYY'),
            format: 'MM/DD/YYYY',
          },
          `Date must be between ${moment()
            .subtract(7, 'day')
            .format('MM/DD/YYYY')} and ${moment()
            .add(7, 'day')
            .format('MM/DD/YYYY')}`
        )
        .typeError('This field is invalid.')
        .required('This field is required.'),
    })}
  >
    <DateRange
      id="dateOfService"
      name="dateOfService"
      min={{ value: 7, units: 'day' }}
      max={{ value: 7, units: 'day' }}
    />
    <Button color="primary" type="submit">
      Submit
    </Button>
  </Form>
</div>;
```

### Props

See [react-dates](https://github.com/airbnb/react-dates#daterangepicker) for additional props

| Prop Name                 | Types                                                                                              | Required | Default                    | Description                                                                                                                                             |
| ------------------------- | -------------------------------------------------------------------------------------------------- | -------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`id`**                  | String                                                                                             | true     |                            | The id of the field.                                                                                                                                    |
| **`name`**                | String                                                                                             | true     |                            | The name of the field. Will be the key of the selected dates that come through in the values of the `onSubmit` callback.                                |
| **`min`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `max` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`max`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `min` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`className`**           | String                                                                                             | false    |                            | Additional styles to be added to the `<DateRange />`                                                                                                    |
| **`disabled`**            | Boolean                                                                                            | false    |                            | Whether the `<DateRange />` is disabled                                                                                                                 |
| **`onChange`**            | Function                                                                                           | false    |                            | Function to be run when date is chosen. Selected value is passed to function in format: `{ value }`                                                     |
| **`onPickerFocusChange`** | Function                                                                                           | false    |                            | Function to be run when focus on the input changes. Value is passed to function in format `{ focused }`                                                 |
| **`calendarIcon`**        | Node                                                                                               | false    | `<Icon name="calendar" />` | Override the default icon that appears                                                                                                                  |
| **`innerRef`**            | Function,Node                                                                                      | false    |                            | Ref passed to `<Input />` inside of `<FormikDate />`.                                                                                                   |
| **`format`**              | String                                                                                             | false    | `"MM/DD/YYYY"`             | How to format date value in `onSubmit` callback. Must be a format recognized by [moment](https://momentjs.com/docs/#/displaying/format/)                |
| **`data-testid`**         | String                                                                                             | false    |                            | Useful for writing unit tests with [@testing-library/react](https://github.com/testing-library/react-testing-library)                                   |
| **`startKey`**            | String                                                                                             | false    | `"startDate"`              | Key to return start date as on form submit. Should match the yup schema `startKey`.                                                                     |
| **`endKey`**              | String                                                                                             | false    | `"endDate"`                | Key to return end date as on form submit. Should match the yup schema `endKey`.                                                                         |
| **`datepicker`**          | Boolean                                                                                            | false    | `true`                     | Toggle whether the calendar is shown                                                                                                                    |
| **`datePickerProps`**     | Object                                                                                             | false    |                            | Props to be spread onto the date range picker component from `react-dates`                                                                              |

## DateRangeField

The same as `DateRange` but with a `Label` that appears above the input and a `Feedback` that appears below the input.

### Usage

```jsx live=true
import React from 'react';
import { Form } from '@availity/form';
import { DateRange } from '@availity/date';
import * as yup from 'yup';
import '@availity/yup';
import '@availity/yup/moment';
import moment from 'moment';

<div className="w-100 d-flex flex-row justify-content-around align-items-center">
  <Form
    initialValues={{
      dateOfService: '',
    }}
    onSubmit={values => console.log(values)}
    validationSchema={yup.object().shape({
      dateOfService: yup
        .dateRange(
          {
            min: moment()
              .subtract(7, 'day')
              .format('MM/DD/YYYY'),
            max: moment()
              .add(7, 'day')
              .format('MM/DD/YYYY'),
            format: 'MM/DD/YYYY',
          },
          `Date must be between ${moment()
            .subtract(7, 'day')
            .format('MM/DD/YYYY')} and ${moment()
            .add(7, 'day')
            .format('MM/DD/YYYY')}`
        )
        .typeError('This field is invalid.')
        .required('This field is required.'),
    })}
  >
    <DateRangeField
      id="dateOfService"
      label="Date of Service"
      name="dateOfService"
      min={{ value: 7, units: 'day' }}
      max={{ value: 7, units: 'day' }}
    />
    <Button color="primary" type="submit">
      Submit
    </Button>
  </Form>
</div>
```

### Props

See [react-dates](https://github.com/airbnb/react-dates#daterangepicker) for additional props

| Prop Name                 | Types                                                                                              | Required | Default                    | Description                                                                                                                                             |
| ------------------------- | -------------------------------------------------------------------------------------------------- | -------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`id`**                  | String                                                                                             | true     |                            | The id of the field.                                                                                                                                    |
| **`label`**               | String                                                                                             | false    |                            | The text that renders inside the `Label` above the input.                                                                                               |
| **`name`**                | String                                                                                             | true     |                            | The name of the field. Will be the key of the selected dates that come through in the values of the `onSubmit` callback.                                |
| **`min`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `max` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`max`**                 | [limitPropType](https://github.com/Availity/availity-react/blob/master/packages/date/src/utils.js) | false    |                            | Used in conjunction with `min` to derive `isOutsideRange` prop from `react-dates`. Dates outside the allowed range will not be clickable in datepicker. |
| **`className`**           | String                                                                                             | false    |                            | Additional styles to be added to the `<DateRange />`                                                                                                    |
| **`disabled`**            | Boolean                                                                                            | false    |                            | Whether the `<DateRange />` is disabled                                                                                                                 |
| **`onChange`**            | Function                                                                                           | false    |                            | Function to be run when date is chosen. Selected value is passed to function in format: `{ value }`                                                     |
| **`onPickerFocusChange`** | Function                                                                                           | false    |                            | Function to be run when focus on the input changes. Value is passed to function in format `{ focused }`                                                 |
| **`calendarIcon`**        | Node                                                                                               | false    | `<Icon name="calendar" />` | Override the default icon that appears                                                                                                                  |
| **`innerRef`**            | Function,Node                                                                                      | false    |                            | Ref passed to `<Input />` inside of `<FormikDate />`.                                                                                                   |
| **`format`**              | String                                                                                             | false    | `"MM/DD/YYYY"`             | How to format date value in `onSubmit` callback. Must be a format recognized by [moment](https://momentjs.com/docs/#/displaying/format/)                |
| **`data-testid`**         | String                                                                                             | false    |                            | Useful for writing unit tests with [@testing-library/react](https://github.com/testing-library/react-testing-library)                                   |
| **`startKey`**            | String                                                                                             | false    | `"startDate"`              | Key to return start date as on form submit. Should match the yup schema `startKey`.                                                                     |
| **`endKey`**              | String                                                                                             | false    | `"endDate"`                | Key to return end date as on form submit. Should match the yup schema `endKey`.                                                                         |
| **`datepicker`**          | Boolean                                                                                            | false    | `true`                     | Toggle whether the calendar is shown                                                                                                                    |
| **`datePickerProps`**     | Object                                                                                             | false    |                            | Props to be spread onto the date range picker component from `react-dates`                                                                              |
