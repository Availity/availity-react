import { Meta, ArgTypes } from '@storybook/addon-docs';
import { hidden_DateField, hidden_DateRange, hidden_DateRangeField } from './date.stories';

<Meta title="Form Components/Date/Additional Props" />

# DateField Props

These props can be passed to the `DateField` component

<ArgTypes of={hidden_DateField} />

# DateRange Props

These props can be passed to the `DateRange` component

<ArgTypes of={hidden_DateRange} />

# DateRangeField Props

These props can be passed to the `DateRangeField` component

<ArgTypes of={hidden_DateRangeField} />
