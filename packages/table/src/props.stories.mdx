import { Meta, ArgTypes } from '@storybook/addon-docs';
import { hidden_avTable } from './Table.stories';

<Meta title="Components/Table/Additional Props" />

# Table Props

These props can be passed to the `Table` component

<ArgTypes of={hidden_avTable} />
