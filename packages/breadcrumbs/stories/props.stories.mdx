import { Meta, ArgTypes } from '@storybook/addon-docs';
import { hidden_RSBreadcrumb, hidden_RSBreadcrumbItem } from './breadcrumbs.stories';

<Meta title="Components/BreadCrumbs/Additional Props" />

# Reactstrap Breadcrumb Props

These props can be passed to the `Breadcrumb` component

<ArgTypes of={hidden_RSBreadcrumb} />

# Reactstrap BreadcrumbItem Props

These props can be passed to the `BreadcrumbItem` component

<ArgTypes of={hidden_RSBreadcrumbItem} />
