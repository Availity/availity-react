import React from 'react';
import { render, cleanup } from '@testing-library/react';
import {{pascalCase packageName}} from '..';

describe('{{pascalCase packageName}}', () => {
  afterEach(() => {
    cleanup();
  });
});
